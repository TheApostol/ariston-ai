Act as an AI routing optimization engineer.

Improve ModelRouter:
- smarter selection logic based on:
  - layer
  - context size
  - task type
- dynamic fallback chains

Add:
- ability to override model via request
- scoring-based routing (future-ready)

Keep it simple but extensible.
